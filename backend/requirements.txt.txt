# Project Labs backend - pinned runtime dependencies
# Matches [project] dependencies in pyproject.toml. Install with:
#   python -m pip install -r requirements.txt
#
# Versions below are those verified in the project venv on 2024-01-17.
# Ranges follow pyproject.toml constraints.

fastapi>=0.110,<1.0
uvicorn[standard]>=0.30,<1.0
sqlalchemy>=2.0,<3.0
alembic>=1.13,<2.0
pydantic>=2.8,<3.0
pydantic-settings>=2.2,<3.0
python-dotenv>=1.0,<2.0
passlib[bcrypt]>=1.7,<2.0
python-jose[cryptography]>=3.3,<4.0
email-validator>=2.1,<3.0
psycopg2-binary>=2.9,<3.0

# ---------------------------------------------------------------------------
# Optional development / test extras (not installed in the current venv)
#
# pytest>=8.0                 # backend test runner
# httpx2>=0.7                 # required by Starlette's TestClient (httpx 2.x
#                             # line; the original httpx 0.x is incompatible)
# ---------------------------------------------------------------------------